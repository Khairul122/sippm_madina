name: CI/CD

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          extensions: mbstring, dom, fileinfo, sqlite3, pdo_sqlite, gd, zip
          coverage: none

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Cache Composer packages
        uses: actions/cache@v4
        with:
          path: vendor
          key: composer-${{ hashFiles('composer.lock') }}
          restore-keys: composer-

      - name: Install Composer dependencies
        run: composer install --no-interaction --prefer-dist --optimize-autoloader

      - name: Install NPM dependencies
        run: npm ci

      - name: Build frontend assets
        run: npm run build

      - name: Prepare environment
        run: |
          cp .env.example .env
          php artisan key:generate

      - name: Run test suite
        run: php artisan test

  deploy:
    name: Deploy via FTP
    needs: test
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          extensions: mbstring, dom, fileinfo, gd, zip
          coverage: none

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install Composer dependencies (production)
        run: composer install --no-interaction --prefer-dist --optimize-autoloader --no-dev

      - name: Install NPM dependencies
        run: npm ci

      - name: Build frontend assets
        run: npm run build

      - name: Deploy start summary
        run: |
          {
            echo "### 🚀 Deploy dimulai"
            echo ""
            echo "| Info | Nilai |"
            echo "|---|---|"
            echo "| Commit | \`${GITHUB_SHA:0:7}\` — $(git log -1 --pretty=%s) |"
            echo "| Branch | \`${GITHUB_REF_NAME}\` |"
            echo "| Trigger oleh | ${GITHUB_ACTOR} |"
            echo "| Waktu (UTC) | $(date -u '+%Y-%m-%d %H:%M:%S') |"
            echo "| Target server | \`${{ secrets.FTP_SERVER }}:21\` |"
            echo "| Target folder | \`${{ secrets.FTP_SERVER_DIR }}\` |"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Deploy to server via FTP
        id: ftp_deploy
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          port: 21
          protocol: ftp
          server-dir: ${{ secrets.FTP_SERVER_DIR }}
          log-level: verbose
          state-name: .ftp-deploy-sync-state.json
          exclude: |
            **/.git*
            **/.git*/**
            **/node_modules/**
            **/tests/**
            **/.github/**
            **/storage/framework/cache/**
            **/storage/framework/sessions/**
            **/storage/framework/views/**
            **/storage/logs/**
            **/.env

      - name: Deploy result summary
        if: always()
        run: |
          {
            echo ""
            echo "### 📦 Hasil deploy"
            echo ""
            if [ "${{ steps.ftp_deploy.outcome }}" = "success" ]; then
              echo "✅ Berhasil di-deploy ke \`${{ secrets.FTP_SERVER }}${{ secrets.FTP_SERVER_DIR }}\`"
            else
              echo "❌ Deploy gagal — cek log step \"Deploy to server via FTP\" di atas untuk detail error FTP."
            fi
            echo ""
            echo "| Metrik | Jumlah |"
            echo "|---|---|"
            echo "| File diunggah | ${{ steps.ftp_deploy.outputs.total-files-uploaded }} |"
            echo "| File dihapus | ${{ steps.ftp_deploy.outputs.total-files-deleted }} |"
            echo "| File dilewati (tidak berubah) | ${{ steps.ftp_deploy.outputs.total-files-skipped }} |"
            echo "| Total file dipindai | ${{ steps.ftp_deploy.outputs.total-files-count }} |"
          } >> "$GITHUB_STEP_SUMMARY"
